$( document ).ready( function() {
  $( "#member-<%= @member.id %>-selectAllMembershipRepositoryIds" ).selectAllCheckbox( {
       checkboxesName   : "member-<%= @member.id %>-membership[repository_ids][]"
    } );
 } );
